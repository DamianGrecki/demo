spring:
  datasource:
    url: jdbc:postgresql://localhost:1010/mydb
    username: root
    password: 123 # It will eventually be moved to .env
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.xml
